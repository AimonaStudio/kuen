export default {
  name: 'titleable',
  props: {
    titleColor: { type: String, default: 'white' },
    titleType: { type: String, default: 'medium' },
    titleShadow: { type: Boolean, default: false },
    title: { type: String, default: '' }
  }
}
